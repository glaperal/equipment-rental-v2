# ------------------------------------------------------------------------------
# Database (Supabase PostgreSQL via Prisma)
# ------------------------------------------------------------------------------
# Get this from your Supabase project's Database settings > Connection string > URI (use the Transaction mode / pgbouncer one for pooling)
# Example: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-SUPABASE-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true
DATABASE_URL=""

# Get this from your Supabase project's Database settings > Connection string > URI (use the Session mode / direct connection one for migrations)
# Example: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-SUPABASE-PROJECT-REF].supabase.co:5432/postgres
DATABASE_DIRECT_URL=""

# ------------------------------------------------------------------------------
# Supabase Auth
# ------------------------------------------------------------------------------
# Get these from your Supabase project's Project Settings > API
# Public URL (used on the client-side)
NEXT_PUBLIC_SUPABASE_URL=""
# Public Anon Key (used on the client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Service Role Key (used securely on the server-side for admin actions if needed, handle with extreme care)
# SUPABASE_SERVICE_ROLE_KEY="" # Optional: Only needed for specific backend operations bypassing RLS

# ------------------------------------------------------------------------------
# Next.js / Application
# ------------------------------------------------------------------------------
# A secret key used for various security purposes (e.g., signing cookies if not handled by Supabase/SSR)
# Generate a strong random string (e.g., using `openssl rand -base64 32`)
# NEXTAUTH_SECRET="" # Usually handled by Supabase SSR, but good practice if using other auth libs

# Base URL of your application (used for absolute URLs, e.g., in emails or callbacks)
NEXT_PUBLIC_APP_URL="http://localhost:3000" # Change for production

# ------------------------------------------------------------------------------
# Optional: Prisma Accelerate / Data Proxy (if using instead of direct/pooled connection)
# ------------------------------------------------------------------------------
# Get these from your Prisma Data Platform project settings if using Accelerate/Data Proxy
# PRISMA_ACCELERATE_API_KEY=""
# PRISMA_DATA_PROXY_API_KEY=""

# Note: Remember to create a `.env` file by copying this file and filling in the actual values.
# DO NOT commit your `.env` file to version control. Add `.env` to your .gitignore file.
